---
"use-custom-event": major
---

Replace zod with standard-schema support. Any validation library that implements the Standard Schema interface (zod 3.24+, valibot, arktype, etc.) can now be used instead of requiring zod specifically.
